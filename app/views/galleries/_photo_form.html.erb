<%= form_with(model: business_profile, local: true) do |form| %>
  
  
  <% if business_profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(business_profile.errors.count, "error") %> prohibited this business_profile from being saved:</h2>
      <ul>
        <% business_profile.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <p class="alert"><%= @err %></p>
  <p><strong> Your Business Logo</strong> </p>
 <div class="row">
    <%= form.label :logo_image %>
    <%= form.file_field :logo_image, class: "form-control" %>
  </div>

<p><strong>  Your Project Photos (allow for selecting multiple photos)<strong> </p>

 <div class="row">
    <%= form.label :images %>
    <%= form.file_field :images, class: "form-control", multiple: true %>
  </div>


  <div class="actions">
    <%= form.submit :class => "btn btn-primary m-3" %>
  </div>

<% end %>
