<%= form_with(model: contact, local: true) do |form| %>
  <% if contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contact.errors.count, "error") %> prohibited this contact from being saved:</h2>

      <ul>
      <% contact.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :mobile_number %>
    <%= form.text_field :mobile_number %>
  </div>

  <div class="field">
    <%= form.label :landline_number %>
    <%= form.text_field :landline_number %>
  </div>

  <div class="field">
    <%= form.label :full_address %>
    <%= form.text_field :full_address %>
  </div>

 <div class="field">
    <%= form.label :business_profile_id %>
    <%= form.text_field :business_profile_id, value:@bp%>
  </div>
<%raise%>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
